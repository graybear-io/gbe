[dependencies]
# The core framework
smithay = { version = "0.3", features = ["backend_winit", "backend_drm", "renderer_glow"] }

# For handling the Wayland protocol communication
wayland-server = "0.31"

# For logging (essential in systems programming)
tracing = "0.1"
tracing-subscriber = "0.3"
