[package]
name = "poc-sentinel"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "poc-sentinel"
path = "src/main.rs"

[dependencies]
chrono = "0.4"
gbe-protocol = { path = "../../crates/protocol" }
libc = "0.2"
serde_json = "1"
